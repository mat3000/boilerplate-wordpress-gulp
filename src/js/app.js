import $ from 'jquery';
// import AjaxNav from './ajax-nav.js';


$(() => {

	// let $progress = $('.progress');
	// let ajaxNav = new AjaxNav({
	// 	container: '.ajax-page', 
	// 	link: '.ajax-link'
	// });


	// $(window).on('load hashchange', () => {

	// 	let page = window.location.hash.replace('#', '');

 //        ajaxNav.load(page, (data) => {

 //        	if(data==0) $progress.css('opacity', 1);
 //        	$progress.css('width', (data*100) + '%');
 //        	if(data==1) $progress.css('opacity', 0);

 //        });

 //    });

});